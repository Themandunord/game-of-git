projects:
  database:
    schemaPath: ../services/git-service/src/prisma/prisma-types.graphql
    extensions:
      endpoints:
        default: http://localhost:4466
      codegen:
        - generator: prisma-binding
          language: typescript
          output:
            binding: ../services/git-service/src/prisma/prisma.binding.ts
