import { LOGIN } from './login';
import { HOME } from './home';
import { REPOSITORIES } from './repositories';
import { REPOSITORY_DASHBOARD } from './repositoryDashboard';
import { ADD_APP_KEY } from './addAppKey';

const ROUTES = [LOGIN, HOME, REPOSITORIES, REPOSITORY_DASHBOARD, ADD_APP_KEY];

export { LOGIN, HOME, REPOSITORIES, ROUTES, REPOSITORY_DASHBOARD, ADD_APP_KEY };
